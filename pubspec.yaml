name: ciel_mobile
description: PicShare / Ciel — Flutter client (iOS + Android).
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  flutter_riverpod: ^2.6.1
  flutter_secure_storage: ^9.2.4
  get_it: ^8.0.3
  go_router: ^14.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  import_lint: ^1.0.1
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
